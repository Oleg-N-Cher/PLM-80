MY:
DO;
PUTCH: PROCEDURE(A) EXTERNAL;
    DECLARE A BYTE;
    END;

CLS: PROCEDURE EXTERNAL; END;

DECLARE (S) ADDRESS;
DECLARE (C BASED S) BYTE;
  CALL CLS;
  S = .('HELLO PL/M', 0);
  DO WHILE C<>0;
    CALL PUTCH(C);
    S = S + 1;
  END;
  DO WHILE 1;
  END;
END;
